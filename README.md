# 訂閱管理 App

GitHub Pages 部署時請上傳下列檔案與資料夾，並保留原有路徑：

- `index.html`
- `manifest-subscription.json`
- `service-worker.js`
- `assets/`
- `.nojekyll`

在 GitHub 儲存庫的 **Settings → Pages**，將來源設為目前分支的根目錄。部署完成後，以：

`https://<你的帳號>.github.io/<儲存庫名稱>/`

開啟訂閱管理器。iPhone Safari 開啟此網址後，選「分享 → 加入主畫面」，即可使用專屬 App 圖示。
